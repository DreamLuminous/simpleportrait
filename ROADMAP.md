# SimplePortrait 简照 · 第一阶段开发规划

> 目标：先把 Web 版证件照主流程做稳定、简单、可验收，再扩展小程序、App 和桌面端。

## 1. 阶段目标

第一阶段的完成标准是：普通用户在电脑浏览器中，不阅读复杂说明，也能在 3 分钟内完成一张可下载的证件照；专业用户可以控制像素、毫米、DPI、背景、格式和文件大小。

本阶段以 Web/PWA 为唯一交付平台，兼顾手机浏览器的基本使用。所有人像处理默认在用户设备内完成，不引入账号、支付或强制联网流程。

## 2. 交付范围

### 必须完成

- JPG、PNG、WebP 导入；电脑拖拽、手机拍照和相册选择。
- 基于归一化坐标的移动、缩放、旋转、居中、适应和重置。
- 预览与最终导出的方向、裁剪位置和颜色保持一致。
- 采集像素与成像区毫米尺寸完全解耦。
- 头部上空、头部区域、肩部区域、左右留空和眼睛线辅助线。
- 保留原背景、跳过抠图、基础本地抠图和纯色换背景。
- 背景取色、HEX/RGB 自定义背景和透明 PNG。
- 四六级默认规格快捷项：浅蓝底、高 192 × 宽 144 px、成像区高 48 × 宽 33 mm、JPG、约 30 KB。
- 文件大小范围与 JPEG 质量搜索；不能满足时明确提示，不偷偷降低像素尺寸。
- 通用文件名提示与模板变量；四六级快捷项不锁定命名方式。
- 普通模式与专业参数折叠；桌面三栏工作台和手机基础布局。
- 中文/English 切换、使用说明、免责声明和本机隐私提示。
- 生成可部署到 Netlify 的静态 `out` 目录。

### 明确不纳入

微信原生小程序、iOS/Android、Windows/Linux、云端 AI 抠图、账号同步、支付会员、广告、批量订单、服装替换、五官重塑和专业打印机色彩管理。

## 3. 用户主流程

```text
导入/拍摄照片 → 选择规格 → 保留背景/抠图/跳过
→ 移动缩放人物 → 选择背景 → 检查成像要求
→ 设置格式和大小 → 输入文件名 → 导出
```

四六级快捷流程应自动应用推荐值，用户可继续修改背景、尺寸、格式、文件大小区间和文件名。

## 4. 技术基线

### 数据分层

```text
原始图片 → 前景/背景 → Transform → Background → OutputSpec → Compression → 文件
```

构图只保存归一化参数，不保存屏幕像素：

```ts
interface Transform {
  centerX: number;       // 0–1
  centerY: number;       // 0–1
  scale: number;
  rotationDeg: number;
}
```

像素、毫米和 DPI 分别保存。改变输出参数不得修改 `Transform`。

### 固定图像管线

```text
解码 → 修正 EXIF 方向 → 人像/背景处理 → 构图 → 背景合成
→ 亮度/对比度 → 输出尺寸 → JPEG/PNG/WebP 编码 → 下载
```

预览可以降采样，但最终导出必须从修正方向后的源图重新渲染。

## 5. 里程碑

### M1 · 编辑核心

- 导入和解码状态完整。
- 拖动、缩放、旋转、方向键微调可用。
- 一键居中、适应、重置可用。
- 画布预览和导出结果一致。

### M2 · 规格和构图

- 四六级、一寸、二寸、身份证、护照和自定义规格配置化。
- `192 × 144 px` 与 `48 × 33 mm` 分离保存。
- 默认构图为 `10% / 70% / 20%`，左右各 `10%`。
- 高级参数可编辑，纵向比例合计有提示。

### M3 · 背景处理

- 保留原背景和跳过抠图路径不受自动抠图影响。
- 本地抠图对纯色和轻微渐变背景可用。
- 背景取色、浅蓝/白/蓝/红/灰、自定义颜色和透明 PNG 可用。
- 边缘缺口、误删和处理失败有可操作提示。

### M4 · 导出和四六级快捷项

- 四六级快捷项首位显示，默认应用浅蓝底、JPG 和 25—35 KB，所有参数可继续修改。
- JPG 质量二分搜索保持最终像素尺寸不变。
- 导出前显示像素、毫米、背景、格式和大小。
- 文件名仅用于本次导出，不作为账号或身份验证信息。

### M5 · 模式和移动端

- 普通模式隐藏复杂参数。
- 专业模式使用独立深色工作台，同时显示规格、成像、画布、抠图、画面和输出参数。
- 手机端无横向滚动，触控目标不小于 44 × 44 px。
- 中英文切换和首次访问提示完成。

### M6 · 文档和发布

- README、ROADMAP、PRIVACY、CHANGELOG 完成。
- `npm run build:netlify` 生成 `out`。
- Netlify Drop 和 Git 自动部署说明完成。

## 6. 验收清单

### 核心正确性

- 修改像素、DPI、JPG 质量和文件大小，人物位置不跳动。
- 同一工程在不同窗口尺寸下，归一化构图结果一致。
- 预览与最终文件的方向、比例、背景和裁剪一致。
- JPEG、PNG、WebP 的扩展名与实际编码一致。

### 四六级场景

- 选中快捷项后显示浅蓝背景。
- 导出尺寸为宽 144 × 高 192 px。
- 成像区摘要为宽 33 × 高 48 mm。
- 构图线为上空 10%、头部 70%、肩部 20%、左右各 10%。
- 默认选择 JPG，目标范围为 25—35 KB，但允许用户按实际要求修改。
- 文件名使用普通输入与模板提示，不强制身份证号。
- 文件名为空时使用安全的默认模板生成。

### 隐私和异常

- 用户照片不请求上传服务器。
- 用户输入的临时文件名不作为身份信息进行校验或统计。
- 无照片、坏文件、权限拒绝、低分辨率和无法达到大小区间时均有提示。
- 断网时仍可完成基本裁剪、调色和导出。

## 7. 第一阶段结束后的下一阶段

第一阶段验收通过后，再拆分第二阶段：更稳定的本地人像分割、微信小程序适配、原生相机/文件系统、Android/iOS 封装、Tauri 桌面端和更完整的规格包。

任何新功能都应先确认不会破坏本阶段的三个不变量：

1. 构图参数与输出像素解耦。
2. 普通用户可以跳过抠图并完成导出。
3. 人像照片默认留在用户设备本地。
